<h1>Bookings</h1>

<%= link_to 'New Booking', new_user_booking_path(@user) %>

<ul>
  <% @user.buddy1_bookings.each do |booking| %>
    <li>
      <%= booking.start_date %> - <%= booking.end_date %> with <%= booking.buddy2.username %>
    </li>
  <% end %>
</ul>
